# Public base URL for the FastAPI backend (must be reachable from the browser)
# Example (local): http://localhost:3001
# Example (cloud): https://<your-backend-host>
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
